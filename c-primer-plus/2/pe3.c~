#include <stdio.h>
int main(void)
{
    int age_years, age_days;
    age_years = 19;
    age_days = age_years * 365;

    printf("Age in years = %d. Age in days = %d\n", age_days, age_years);

    return 0;
}
